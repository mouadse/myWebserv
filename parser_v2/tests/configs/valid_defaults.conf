# Minimal config to ensure defaults and inheritance behave
server {
    listen 8092;
    root ./www;

    # host, index, server_name, and max body size are intentionally omitted
    location / {
        allow_methods GET DELETE;
    }

    location /errors {
        root ./www/errors;
        index 404.html;
        autoindex on;
    }
}
